[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# On garde uniquement les erreurs de syntaxe, de bugs potentiels et le tri des imports
select = ["E", "F", "I"]

# Desactivation des regles trop strictes
ignore = [
    "E501", # Lignes longues
]